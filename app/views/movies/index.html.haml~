-# This file is app/view/movies/index.html.haml
%ol.breadcrumb
  %li All Movies

%table#movies.table.table-hover
  %thead
    %tr
      %th Movie Title
      %th Rating
      %th Release Date
      %th More Info
  %tbody
    - @movies.each do |movie|
      %tr
        %td= movie.title
        %td= movie.rating
        %td= movie.release_date.strftime("%d-%B-%Y")
        %td= link_to "More about #{movie.title}", movie_path(movie)

= link_to 'Add new movie', new_movie_path, :class=>"btn btn-success btn-large"
